# Smart'Design System v1.0

Système de design unifié pour la plateforme Smart'Building.

## Structure

- `tokens/` - Variables CSS (couleurs, typographie, espacements)
- `components/` - Composants HTML/CSS réutilisables
